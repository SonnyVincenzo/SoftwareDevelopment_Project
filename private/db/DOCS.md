
# SQL [folder](./sql/)
Use of SQL folder is to have it as a reference point of how the data conforms to (tables, variables, attributes).
Not a use of schemas, only a reference point.
(Could potentially change the folder to make it useable)